{
  "compilerOptions": {
    /* JavaScript Support */
    "target": "es2016",                          /* Set the JavaScript version to be output. Can be 'es2020' or 'esnext' for more modern features. */
    "module": "commonjs",                        /* Module code generation. "esnext" or "es6" might also be used for ES Modules. */
    "jsx": "react-jsx",                          /* React JSX transform for React 17+ */
    "moduleResolution": "node",                  /* Ensure Node-style module resolution */
    "esModuleInterop": true,                     /* Allow default imports from CommonJS modules */
    "forceConsistentCasingInFileNames": true,    /* Ensure imports are case-sensitive (cross-platform) */
    
    /* Type Checking */
    "strict": true,                              /* Enable all strict type-checking options. */
    "skipLibCheck": true,                        /* Skip type checking for libraries to improve build time */
    
    /* Interop Constraints */
    "resolveJsonModule": true,                   /* Allow importing JSON files */
    "allowSyntheticDefaultImports": true,        /* Allow synthetic default imports for CommonJS modules */
    
    /* Emit */
    "sourceMap": true,                           /* Emit source maps for easier debugging */
    "outDir": "./dist",                          /* Output directory for compiled files */
    
    /* Optional additional settings (remove comments to enable) */
    // "declaration": true,                        /* Generate `.d.ts` declaration files */
    // "skipDefaultLibCheck": true,                /* Skip type checking of default library files */
    // "noEmitOnError": true,                      /* Prevent emitting JavaScript files when there are type errors */
  },
  "include": [
    "src/**/*"                                   /* Include all files in the src directory */
  ]
}
